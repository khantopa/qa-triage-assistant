# QA Triage Report

**Generated**: 2026-03-13T03:25:05Z
**Run**: Run 1
**Branch**: QA: `feature/release-ercole-run1fixes` | FE: `release/ercole` | BE: `release/ercole`
**Build Commit**: N/A (not found in ZIP)
**Report generated**: 2026-03-12 12:14:30

## Summary

- Total failures: 1
- New regressions (Critical): 1
- Existing issues (Full/Quick): 0
- Chronic failures (Skipped): 0
- FE Code: 1 | BE Code: 0 | QA Automation: 0 | CI/Config: 0 | Inconclusive: 0

## Failures

### 1. Validate Nickname field attempts on Edit Profile And PAS Queue

- **Triage Tier**: Critical (first-time failure — not in history)
- **Classification**: FE Code
- **Confidence**: MEDIUM
- **Error**: `User should see 'Complete now and get a FREE Boost' on the Profile Wall`
- **Stack Trace** (key frames):
  ```
  incube8.seeking.definitions.profile.edit.ProfileWallDefinitions.andUserShouldSeeCompleteNowAndGetAFREEBoostOnTheProfileWall(ProfileWallDefinitions.java:154)
  Nickname_Attempts.feature:42
  ```
- **Failing Step Context**: After a PAS moderator denies a user's first name, the test navigates to the Member page and expects a "Complete now and get a FREE Boost" modal (rendered as `div#free_profile_boost_count_down_container` inside the Profile Wall / PCRBar). This element is not found.

- **Sprint Trigger**: `227a0ca07c` by Thet Naing — `"SATHREE-41561: Modal manager development and first batch migration"` — merged 2026-03-09 — a large FE refactor (5,951 insertions across 46 files) that restructured how modals and the PCRBar (`AfterLoginPCR.tsx`) are rendered. The "Complete now and get a FREE Boost" text lives in `AfterLoginPCR.tsx`, which was directly modified in this commit.

- **Root Cause** (if different): Same as sprint trigger. The modal manager migration changed the rendering lifecycle/sequencing of the PCRBar and profile wall modals, causing the `free_profile_boost_count_down_container` div to either not render or not be present at the time the test asserts.

- **Chain of Causation**:
  > 1. `SATHREE-41561` (2026-03-09) introduced a new `ModalManager` system and migrated `AfterLoginPCR.tsx` as part of the first batch. This changed how/when the "Complete now and get a FREE Boost" element is rendered on the Member page.
  > 2. The QA step (`ProfileWallDefinitions.java:154`) asserts on `div#free_profile_boost_count_down_container` — an element that either no longer exists with that ID, renders conditionally, or is gated behind new modal manager logic.
  > 3. A QA fix attempt was made in `de9f0d3497` (2026-03-12) adding `WaitUntil.refreshIfElementIsUnstable` — but the element is still not found, indicating the issue is structural (FE rendering change), not a timing/wait problem.

- **Evidence**:
  - `AfterLoginPCR.tsx` contains "Complete now and get a FREE Boost" text (FE source confirmed)
  - `227a0ca07c` modified `AfterLoginPCR.tsx` with 78 line changes as part of modal manager migration
  - QA selector: `(//div[@id='free_profile_boost_count_down_container'])[1]` — hardcoded ID not found post-migration
  - QA fix attempt `de9f0d3497` (2026-03-12) did not resolve the failure
  - Test step sequence is correct — PAS denial → navigate to member page → assert boost CTA modal

- **Root Cause (Detailed)**:
  Before `SATHREE-41561`, `AfterLoginPCR` was always mounted in the DOM but used a local `showModal` state (`useState(false)`) — something triggered `setShowModal(true)` to make it visible. After the migration, the component no longer manages its own visibility; the modal manager gates rendering entirely — `AfterLoginPCR` is only mounted when the modal manager's conditions are satisfied.
  Additionally, even when mounted, `div#free_profile_boost_count_down_container` only renders when `hasCountDown` is `true`, which requires `user.reg_boost_eligible_ended_at` to be set and non-zero. If the test user setup does not populate this field, the element will never appear regardless of the modal manager.

- **Recommended Fix**:
  - **FE team**: The modal manager migration (`SATHREE-41561`) changed `AfterLoginPCR` from always-mounted-but-hidden to only-mounted-when-rules-are-met. The test was passing before this change with the same setup, so the modal manager's mounting rules do not cover the scenario this test exercises (newly registered unapproved user on Member page after PAS denial). Fix the modal manager rule so `AfterLoginPCR` mounts under the same conditions it previously appeared.
  - File: `sa-v3/resources/react-app/components/memberDashBoard/PCRBar/AfterLoginPCR.tsx` and the modal manager rule governing its mounting.
  - **No QA changes required** — the test setup was correct before `SATHREE-41561` and should not need to change.

- **Recommended Owner**: FE squad (SATHREE-41561 / Thet Naing).

---

## Action Items

- [ ] FE (Thet Naing / SATHREE-41561 squad): Fix modal manager mounting rules so `AfterLoginPCR` renders for a newly-registered unapproved user on the Member page after PAS denial — same conditions as before the migration
- [ ] QA: Revert `de9f0d3497` once FE fix is confirmed — `WaitUntil.refreshIfElementIsUnstable` was a workaround for a structural FE rendering issue, not a timing problem

# QA Triage Report

**Generated**: 2026-03-13T10:32:45
**Run**: Run 1 (Thanos / Profile Auto-Moderation suite)
**Branch**: QA: `feature/release-ercole-run1fixes` | BE: `release/ercole`
**Build Commit**: N/A (not found in report artifacts)
**Report Date**: 2026-03-12 12:28:15

---

## Summary

- Total tests: 1
- Failed with errors: 1
- New regressions (Critical): 1 (first-seen failure, no prior history)
- FE Code: 0 | BE Code: 0 | QA Automation: 0 | CI/Config: 0 | Inconclusive: 1

---

## Failures

### 1. Validation Not Met - Username criteria for Profile Auto-Moderation - Attractive Member (Auto-Approved after First Name Approval)

- **Story**: TC [PAS-AUTO-MODERATE] - Validate Profile Auto-Moderation by utilizing Thanos - Attractive & Generous
- **Triage Tier**: Critical (first-seen failure — no history entry)
- **Classification**: Inconclusive — shared QA Automation + BE interaction
- **Confidence**: MEDIUM
- **Error**: `NoSuchElementException` — XPath `//strong[text()='Looking For']/../..//span[contains(normalize-space(.),'Reason')]` not found in DOM
- **Stack Trace** (key frames):
  ```
  incube8.seeking.definitions.admin.profile.AdminProfileContentDefinitions.shouldSeeDragynReason(AdminProfileContentDefinitions.java:264)
  Feature: Fraud_ML_Profile_Moderation.feature:110
  ```

---

### Root Cause Chain

**What the test does:**
1. ✅ Updates profile with mock moderation: `{"username": "JJJXXQQ"}`, decision: `MODERATE`
2. ✅ Navigates to Dragyn History tab — sees Dragyn recommendations for all fields
3. ✅ Admin Profile page: sees Pending Profile Updates for "Looking For"
4. ✅ Sees `Dragyn Recommendation` for "Looking For" (MODERATE) — `<span>` with "Recommendation" found
5. ❌ Sees `Dragyn Reason` for "Looking For" — `<span>` with "Reason" **not found**

**Why the Reason span is absent:**

In `resources/views/admin/partials/approvals.blade.php`, the Reason is rendered conditionally:
```php
@if(!empty($approval['dragyn']['reason']))
    Reason: {{ $approval['dragyn']['reason'] }}</span>
@endif
```
The span is only shown when `$approval['dragyn']['reason']` is non-empty. In this test run, `reason` is empty — so the span is never inserted in the DOM.

**Why reason is empty:**

The mock step `API - Mock moderation response using details as '{"username": "JJJXXQQ"}' with decision 'moderate'` calls:
```java
profileUpdateApiSteps.mockModerationResponse(detailsJsonString, decision, "Mock");
// → requestParams.put("qa_reason", "Mock");  // non-empty reason
```

However, the BE processes text profile updates via `UpdateProfileEventRequest::handle()`, which in QA test sync mode calls:
```php
ProcessDragynTextAssessment::dispatchSync($data, $pasId);  // ← requestData NOT passed
```
The third parameter `Dragyn::getRequestData()` (containing `qa_reason`/`qa_recommendation`) is only included in the async queue path:
```php
ProcessDragynTextAssessment::dispatch($data, $pasId, Dragyn::getRequestData())->onConnection('redis');
```

In sync dispatch mode, `$this->requestData = []` inside the job, so `qa_reason` never reaches the Dragyn service. The **real Dragyn service** is called instead of the mock. The real service returns a MODERATE recommendation for "JJJXXQQ" but with an **empty reason field** in the test environment.

The Dragyn Recommendation span still passes because the `<span>` for Recommendation is shown unconditionally when `recommendation` is non-empty — and the real service always returns a recommendation.

---

- **Sprint Trigger**: No direct code change in the 14-day sprint window matches this failure exactly.
  - Closest candidate: `1cf947eb02` (Feb 26, 2026, SATHREE-41335) — "QA override no longer bypasses canAutoDenyContent rule" — changed `UpdateProfileEventCallback.php` (dropped `$request` param from `eventCanAutoDeny()`). May have altered side-effects of the QA override path, but no direct link to `reason` being empty is confirmed.
  - The `dispatchSync` omitting `requestData` is a **pre-existing structural issue** that may have been masked previously (e.g. if the real Dragyn service previously returned a reason for similar mocked inputs, or if this scenario was never exercised on the current branch).

- **Root Cause**: `ProcessDragynTextAssessment::dispatchSync()` does not receive `Dragyn::getRequestData()`, so `qa_reason` never flows into the Dragyn payload for sync-mode text assessments. The real Dragyn service is called with only the profile content (no QA override), and returns an empty `reason` for the MODERATE verdict on "JJJXXQQ".

- **Chain of Causation**:
  1. `UpdateProfileEventRequest::handle()` calls `dispatchSync($data, $pasId)` without `requestData` → `qa_reason` lost
  2. `ProcessDragynTextAssessment::sendApprovalToDragyn()` calls `$dragyn->moderation($payload)` without `qa_override`/`qa_reason`
  3. Real Dragyn processes "JJJXXQQ" → returns MODERATE + empty reason
  4. `UpdateProfileEventCallback` stores empty reason in DB
  5. `approvals.blade.php` hides Reason span (empty string → condition false)
  6. `shouldSeeDragynReason()` XPath lookup throws `NoSuchElementException`

---

### Evidence

- `AdminProfileContentDefinitions.java:264` — `getProfileTextDragynReason(field)` using XPath for `<span>` with "Reason"
- `AdminProfileTabComponent.java:746-748` — XPath: `//strong[text()='<fieldLabel>']/../..//span[contains(normalize-space(.),'Reason')]`
- `resources/views/admin/partials/approvals.blade.php:17-18` — `@if(!empty($approval['dragyn']['reason']))` gate
- `app/Repositories/Dragyn/RequestProcesses/UpdateProfileEventRequest.php` — `dispatchSync($data, $pasId)` omits `requestData`
- `app/Jobs/ProcessDragynTextAssessment.php:42` — constructor: `array $requestData = []` (defaults to empty in sync)
- `app/Services/Dragyn.php:820` — `getDefaultMockResponse()` reads from `$this->payload['qa_reason']` — never populated in sync mode
- Test step: preceding step "Dragyn Recommendation for Looking For" ✅ PASSES (Recommendation span always rendered when non-empty); "Dragyn Reason for Looking For" ❌ FAILS (Reason span conditionally gated)
- No sprint-window (14-day) changes to `approvals.blade.php`, `ProcessDragynTextAssessment.php`, or `UpdateProfileEventRequest.php`
- QA sprint-window change `7bec94945e` (SATHREE-41718, Mar 5) is only a Serenity package import rename — not causal

---

### Recommended Fix

**Option A — Fix the root cause in BE (recommended):**
- `app/Repositories/Dragyn/RequestProcesses/UpdateProfileEventRequest.php`
  - Change `dispatchSync($data, $pasId)` → `dispatchSync($data, $pasId, Dragyn::getRequestData())`
  - This ensures `qa_reason`/`qa_recommendation` flow into the job in both sync and async paths

**Option B — Fix the test to assert conditionally:**
- `QA: AdminProfileContentDefinitions.java` — change `shouldSeeDragynReason` to use `shouldNotSeeDragynReason` instead, or update the feature file to use the step that allows specifying a reason: `API - Mock moderation response using details as '...' with reason '<reason>' decision 'moderate'`
- Note: Option B papers over the real issue (QA override not working for text assessments in sync mode)

**Option C — Investigate why reason is empty from real Dragyn:**
- Verify in the QA environment whether the real Dragyn text assessment endpoint returns a `reason` field for MODERATE decisions
- If it should return a reason but doesn't, this is a Dragyn service configuration or API contract issue

---

### Recommended Owner

- **BE team** (primary) — fix `dispatchSync` to pass `requestData`
- **QA Automation** (secondary) — consider using the reason-aware mock step as a workaround

---

## Action Items

- [ ] **BE**: Fix `UpdateProfileEventRequest::handle()` to pass `Dragyn::getRequestData()` in `dispatchSync` path — mirrors the queue path
- [ ] **QA**: Verify whether the real Dragyn text service returns a `reason` for MODERATE decisions in QA environment
- [ ] **QA**: If BE fix is not immediate, update feature file to use `API - Mock moderation response using details as '...' with reason 'Mock' decision 'moderate'` step instead
- [ ] **Team**: Confirm if SATHREE-41335 (Feb 26 QA override change) contributed any indirect effect on this flow

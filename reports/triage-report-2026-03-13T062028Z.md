# QA Triage Report

**Generated**: 2026-03-13T06:20:28Z
**Updated**: 2026-03-13T07:30:00Z (revised — QA Automation root cause confirmed)
**Run**: Run 1 (Eye Verification)
**Branch (BE)**: release/ercole
**Branch (QA)**: feature/release-ercole-run1fixes
**Build Commit**: e534d70207 (BE HEAD at time of run)

---

## Summary

- Total failures: 1
- New regressions (Critical): 1
- FE Code: 0 | BE Code: 0 | QA Automation: 1 | CI/Config: 0 | Inconclusive: 0

---

## Failures

### 1. Validate Re-moderation for Eye Verification

- **Triage Tier**: Critical (first-time failure — no history)
- **Pattern**: `config-setup-inversion`
- **Classification**: QA Automation
- **Confidence**: HIGH
- **Error**: `PAS Moderator should see new user record with Email Masked in Moderation Queue`
- **Stack Trace** (key frames):
  ```
  incube8.seeking.definitions.admin.pas.AdminPasModerationDefinitions
    .pasModeratorShouldSeeNewUserRecordInModerationQueue(AdminPasModerationDefinitions.java:47)
  ✽.PAS Moderator should see new user record in Moderation Queue
    (The_Eye_Verification.feature:20)
  ```

#### Failure Anatomy

The `assertThat` message "PAS Moderator should see new user record with Email Masked in Moderation Queue" is a step label — not the name of the missing element. The actual failure is the profile never appearing in the Escalated Profile Queue because the Probational Approval feature was disabled by a buggy setup step.

The test flow:

1. `API - Update 'Auto Moderate Settings' for 'PROBATIONAL_APPROVAL_ENABLED' as 'Enabled'` ❌ **BUG HERE**
   - This step sends `PUT {"value":false}` to Zeus config 504 — disabling Probational Approval
   - With Probational Approval OFF, no profile can ever be routed to the Escalated Profile Queue
2. New user registered (profile id N003) ✅
3. PAS Moderator → New Profile Moderation Queue → sees user ✅
4. PAS Moderator submits "Submit Decision for all Above" ✅
5. PAS Moderator navigates to **Escalated Profile Queue** ❌
   - `refreshPageUntilElementAppears(endWorkButtonModerationQueue)` times out
   - Profile cannot appear because escalation is disabled

#### Passing vs Failing Setup

| Setup Step                                        | Passing Run (Mar 6)     | Failing Run (Mar 13)     |
| ------------------------------------------------- | ----------------------- | ------------------------ |
| `selfie.isMandatorySelfieByEvidently`             | `PUT {"value":true}`    | Not set                  |
| `moderation.probationApproval.isEnabled` (id 504) | `PUT {"value":true}` ✅ | `PUT {"value":false}` ❌ |
| `moderation.profile.isAutoSuspendEnabled`         | `PUT {"value":false}`   | Not set                  |
| `moderation.profile.autoSuspendValue`             | `PUT {"value":85}`      | Not set                  |

The single critical difference: Probational Approval was **enabled** in the passing run and **disabled** in the failing run, despite the step being named `as 'Enabled'`.

- **Sprint Trigger**: `b8957e82bb` by Kamna Pamnani — `SATHREE-41696: Updtaed for testing` — **2026-03-06 17:30 SGT**

```diff
- adminApiConfigurationSteps.updateAutoModerateSettingsAs(configEnumName, true);
+ adminApiConfigurationSteps.updateAutoModerateSettingsAs(configEnumName, false);
```

**File**: `AdminApiConfigurationDefinitions.java`, step `API - Update 'Auto Moderate Settings' for '(.+)' as 'Enabled'`

This commit inverted the boolean — a step named `as 'Enabled'` now sends `false`. The commit message "Updated for testing" suggests this was a temporary debug change that was never reverted before the test was promoted to the run suite.

- **Root Cause** (same as sprint trigger): Boolean inverted in step definition — `as 'Enabled'` sends `false`, `as 'Disabled'` sends `true`.

#### Why HIGH confidence

1. **Console log diff is conclusive**: Passing run → `{"value":true}`, failing run → `{"value":false}` for the same Zeus config, same step name.
2. **Git diff confirms the inversion**: `b8957e82bb` changed `true` → `false` on the exact line in the exact method.
3. **No BE code change needed**: Probational Approval escalation works correctly (passing run Mar 6 proves this). The logic just never ran because the flag was disabled.
4. **Feature file content is identical**: `f619c5db61` renamed the file but the `@Re-moderation` scenario active steps are byte-for-byte the same. File rename is not the cause.

#### Evidence

- **Failing console log**: Step "API - Update 'Auto Moderate Settings' for 'PROBATIONAL_APPROVAL_ENABLED' as 'Enabled'" → `PUT /api/v1/zeus/platform-config/504` with body `{"value":false}`
- **Passing console log (Mar 6)**: Same step → `PUT` with `{"value":true}`
- **`b8957e82bb` diff**: `AdminApiConfigurationDefinitions.java:30` — `true` changed to `false`
- **Author**: Kamna Pamnani (same person who wrote the failing feature file `f619c5db61`)
- **Passing run (Mar 6)**: Old feature file path, profile appears in Escalated Queue ✅

#### Recommended Fix

`AdminApiConfigurationDefinitions.java:30` — revert `false` back to `true`:

```java
// BEFORE (broken):
adminApiConfigurationSteps.updateAutoModerateSettingsAs(configEnumName, false);

// AFTER (fix):
adminApiConfigurationSteps.updateAutoModerateSettingsAs(configEnumName, true);
```

#### Recommended Owner

**QA (Kamna Pamnani)** — her commit `b8957e82bb` introduced the inversion.

---

## Action Items

- [ ] **QA (Kamna Pamnani)**: Revert `b8957e82bb` change in `AdminApiConfigurationDefinitions.java:30` — change `false` back to `true` in `updateAutoModerateEnabled()`
- [ ] **QA**: Verify the fix by re-running "Validate Re-moderation for Eye Verification" in testqa with Probational Approval properly enabled
- [ ] **QA**: Check if any other tests use `API - Update 'Auto Moderate Settings' for '(.+)' as 'Enabled'` — they will all be broken by this inversion

# QA Triage Report

**Generated**: 2026-03-13T02:57:25Z
**Run**: Run 1
**Branch**: QA: feature/release-ercole-run1fixes | BE: release/ercole | FE: release/ercole (not used)
**Build Commit**: N/A (not found in ZIP)
**Test Run Date**: 2026-03-11 10:54:09

## Summary

- Total failures: 1
- New regressions (Critical): 1
- Existing issues (Full/Quick): 0
- Chronic failures (Skipped): 0
- FE Code: 0 | BE Code: 0 | QA Automation: 1 | CI/Config: 0 | Inconclusive: 0

## Failures

### 1. Validate ID Verification Underage Improvements - Underage Registration

- **Story**: TC [PAS] - ID Verification Queue Improvements
- **Triage Tier**: Critical (first-time failure)
- **Classification**: QA Automation
- **Confidence**: HIGH
- **Error**: `PAS Moderator should see 'Underage at Account Creation' as Yes — Expected: a string containing "Yes" but: was "No"`
- **Stack Trace** (key frames):
  ```
  AdminPasIDVModerationDefinitions.pasModeratorShouldSeeUnderageAtAccountCreationAs(AdminPasIDVModerationDefinitions.java:79)
  ID_Verification_Improvements.feature:146
  ```

- **Sprint Trigger**: No code change. This is a **date-boundary failure** triggered by running the test on 2026-03-11 — a date where the QA formula produces an exact 18-year boundary.
- **Root Cause**: Leap-year-sensitive arithmetic in `AdminPASApiSteps.java`:
  ```java
  // Line ~57 in AdminPASApiSteps.java
  case "UNDERAGE_REGISTRATION":
      idvBirthdate = today.minusYears(19).plusDays(offsetDays); // offsetDays = 356
  ```
  - `today = 2026-03-11`
  - `today - 19 years = 2007-03-11`
  - `2007-03-11 + 356 days = 2008-03-01` ← spans the 2008 leap year (Feb 29), consuming 1 extra day
  - `account_created = today - 10 days = 2026-03-01`
  - `DateDifference("2026-03-01", "2008-03-01") = exactly 18 years`
  - `18 < 18 = false` → **"No"**

- **Chain of Causation**: The formula `today - 19 years + 356 days` is anchored to `today`, not to `account_created`. When the 19-year backward span crosses a leap-year February (as it does from 2007→2008), the resulting date lands exactly 18 years before `account_created`. The BE logic uses a strict `< 18` check, so the boundary case returns `false`. The test was previously passing because on other run dates, the arithmetic produces 17y 364d or similar — but on 2026-03-11, it lands exactly on the boundary.

- **Evidence**:
  - `AdminPASApiSteps.java`: `idvBirthdate = today.minusYears(19).plusDays(356)` → produces `2008-03-01`
  - Test modifies account_created to `today - 10 days = 2026-03-01`
  - BE `isUnderageAtAccountCreation`: `DateDifference("2026-03-01", "2008-03-01").years = 18` → `18 < 18 = false`
  - BE logic unchanged since Aug 2025 (commit `777814b49e` / SATHREE-40167, Jianzi)
  - QA step definition unchanged since Aug 2025 (commit `33e84ccbbc0` / SATHREE-40199, Kamna Pamnani)
  - No sprint commits in either repo touched this code path within last 14 days

- **Recommended Fix**:
  - `AdminPASApiSteps.java` (UNDERAGE_REGISTRATION case): Anchor birthdate to `account_created` instead of `today`, or add a safety buffer
  ```java
  // Option A — anchor to account creation date (most accurate)
  LocalDate accountCreated = today.minusDays(10); // match the 10-day offset used in test setup
  idvBirthdate = accountCreated.minusYears(18).plusDays(1); // 1 day underage at account creation

  // Option B — quick fix: increase offset by 2 days to clear leap-year boundary
  idvBirthdate = today.minusYears(19).plusDays(offsetDays + 2); // 358 days instead of 356
  ```
  - Option A is preferred — it directly expresses the intent (1 day underage at account creation) and is immune to leap-year drift.

- **Recommended Owner**: QA Automation (sa-ui-automation team — Kamna Pamnani or current maintainer)

---

## Action Items

- [ ] Fix `AdminPASApiSteps.java` UNDERAGE_REGISTRATION birthdate formula to anchor to account_created rather than today
- [ ] Re-run the test after fix to confirm it passes consistently across date boundaries
- [ ] Consider adding a comment in the formula explaining the leap-year sensitivity

---

## Hypothesis Engine Note

The `face-comparison` pattern matched on error signature `"Expected: a string containing"` (score: 3) but with zero keyword matches (no selfie/IPCF/liveness/compare-faces context). This was correctly identified as a **false positive** and standard investigation was run. The `"Expected: a string containing"` signature should be removed from the face-comparison pattern or require additional keyword co-occurrence to avoid future false positives.
